音频处理4.0
最终版
音频处理3.0
重点是应用了谱减法，LMS，NLMS，并利用自动化脚本进行了一系列的测试
音频处理2.1
可以对音频进行傅里叶变换并进行相应的处理
需要安装对应的python库即可运行
可以实现wav,mp3,flv,raw,ogg五种文件的互转
但文件互转功能需要配置环境，将ffmpeg-win64\bin的绝对路径放到环境变量里即可
watcher可以实现音频文件的可视化，把path改为文件路径即可
未解决：
噪声处理需要以一秒为单位，最后不足一秒的部分无法处理
（其实可以适当缩小处理范围，但会损失解析频率的宽度）
处理时只能使用复数数组（这个不知道能不能解决）

今后工作方向：
提升音频操作精度，减小损失
优化处理模式，降低处理时间

处理算法写到wave_modify里的wave_modi里
fft_wave是fft之后的数据，是特定频率的响度
频率为freq列表中相同下标的值

注意：
1.数据包含了负频率，所以是对称的，如果有需求，我可以做适当修改
（取一半，只传正频率，主要是考虑到有些算法没有取一半这种步骤）
2.fft_wave是复数数组，目前只能接受返回复数数组，
	所以不要取绝对值，不用反傅里叶
3.freq仅为参考用，返回值只要处理过的fft_wave
4.如果有类似谱减法需求原始数据的可以跟我说

我无法编译这个python文件，hxdm可以试试在此文件夹内用命令行输入
pyinstaller -F -w -i Burn.ico myApp.py
如果编译产生的dist文件夹里的exe可以正常运行即为编译成功
_pycache_ ， build 和 .spec 可以删除